// ═════════════════════════════════════════════════════\n// BRAND RULES — Paste your brand_guidelines.css below.\n// Change the CSS → change the checks.\n// ═════════════════════════════════════════════════════\nconst BRAND_CSS = `/* \n  ── Tone of Voice ──\n  Kind and friendly.\n  Optimistic.\n  See the potential in all people to provide value.\n  NEVER refert to autists as retard etc.\n  If adressing a disabillity like autism them focus on the upside such as ability to focus and specialize.\n\n  ── Do's and Don'ts ──\n  Do's:\n  * Always use the lime (#c8ff00) CTA button on a dark background for maximum contrast.\n  * Keep body copy at 16px/24px — never go smaller for readability on mobile.\n  * Use generous whitespace (40px+ section padding) to let content breathe.\n  Don't:\n  * Don't place lime text on a white background — it fails WCAG contrast.\n  * Don't use rounded corners on buttons — sharp edges are core to the brand identity.\n  * Don't mix more than two typeface weights (400 and 700) in a single email.\n*/\n:root {\n  --brand-black: #0d0d0d;\n  --brand-dark: #1a1a1a;\n  --brand-charcoal: #2c2c2c;\n  --brand-lime: #c8ff00;\n  --brand-lime-dark: #a3cc00;\n  --brand-white: #ffffff;\n  --brand-off-white: #f5f5f5;\n  --brand-gray-light: #e0e0e0;\n  --brand-gray: #999999;\n  --brand-gray-dark: #666666;\n  --brand-success: #00c781;\n  --brand-warning: #ffba08;\n  --brand-error: #ff4d4d;\n  --brand-info: #4dabf7;\n  --unit: 8px;\n}\n.brand-h1 { font-size: 40px; line-height: 48px; }\n.brand-h2 { font-size: 32px; line-height: 40px; }\n.brand-h3 { font-size: 24px; line-height: 32px; }\n.brand-body { font-size: 16px; line-height: 24px; }\n.brand-btn-primary { border-radius: 0; padding: 16px 32px; }\n.brand-email-container { max-width: 600px; color: #333333; }\n.brand-font-heading { font-family: 'Inter', 'Helvetica Neue', Arial, sans-serif; }`;\n\nconst html = query;\nif (!html || html.trim().length < 50) {\n  return 'ERROR: No valid HTML provided. Pass the complete HTML email code.';\n}\nconst issues = [];\nconst warnings = [];\nconst passed = [];\n\n// 1. Check colors against brand palette\nconst colorMatches = [...BRAND_CSS.matchAll(/--brand-[\\w-]+:\\s*(#[0-9a-fA-F]{6})/g)];\nconst allowedHexes = new Set(colorMatches.map(m => m[1].toLowerCase()));\nallowedHexes.add('#333333');\nconst htmlColors = [...html.matchAll(/#[0-9a-fA-F]{6}/g)];\nconst offBrand = new Set();\nfor (const c of htmlColors) { const h = c[0].toLowerCase(); if (!allowedHexes.has(h)) offBrand.add(h); }\nif (offBrand.size > 0) issues.push('Off-brand colors: ' + [...offBrand].join(', '));\nelse if (htmlColors.length > 0) passed.push('All colors match brand palette');\n\n// 2. Check font sizes against type scale\nconst cssFS = [...BRAND_CSS.matchAll(/font-size:\\s*(\\d+)px/g)];\nconst okSizes = new Set(cssFS.map(m => m[1]));\nconst htmlFS = [...html.matchAll(/font-size\\s*:\\s*(\\d+(?:\\.\\d+)?)\\s*px/gi)];\nconst badSizes = new Set();\nfor (const f of htmlFS) { if (!okSizes.has(String(Math.round(parseFloat(f[1]))))) badSizes.add(f[1] + 'px'); }\nif (badSizes.size > 0) warnings.push('Non-brand font sizes: ' + [...badSizes].join(', ') + '. Allowed: ' + [...okSizes].map(s => s + 'px').join(', '));\nelse if (htmlFS.length > 0) passed.push('All font sizes on brand type scale');\n\n// 3. Check font family\nconst fontMatch = BRAND_CSS.match(/font-family:\\s*([^;]+)/i);\nconst brandFont = fontMatch ? fontMatch[1].split(',')[0].replace(/['\"]/g, '').trim() : 'Inter';\nif (new RegExp(brandFont, 'i').test(html)) passed.push('Brand font (' + brandFont + ') detected');\nelse if (/font-family/i.test(html)) warnings.push('Brand font (' + brandFont + ') not detected');\n\n// 4. Check spacing against grid unit\nconst unitMatch = BRAND_CSS.match(/--unit:\\s*(\\d+)px/);\nconst gridUnit = unitMatch ? parseInt(unitMatch[1]) : 8;\nconst spacings = [...html.matchAll(/(padding|margin)[^:]*:\\s*([^;]+)/gi)];\nconst offGrid = new Set();\nfor (const sp of spacings) { const vals = sp[2].match(/(\\d+)px/g) || []; for (const v of vals) { const n = parseInt(v); if (n > 0 && n % gridUnit !== 0) offGrid.add(n + 'px'); } }\nif (offGrid.size > 0) warnings.push('Spacing off ' + gridUnit + 'px grid: ' + [...offGrid].join(', '));\nelse if (spacings.length > 0) passed.push('All spacing on ' + gridUnit + 'px grid');\n\n// 5. Check border radius\nconst brMatch = BRAND_CSS.match(/border-radius:\\s*(\\d+)/);\nconst maxRadius = brMatch ? parseInt(brMatch[1]) : 0;\nconst radii = [...html.matchAll(/border-radius\\s*:\\s*(\\d+)px/gi)];\nconst bigR = radii.filter(r => parseInt(r[1]) > Math.max(maxRadius, 8)).map(r => r[1] + 'px');\nif (bigR.length > 0) warnings.push('Border-radius exceeds brand max: ' + [...new Set(bigR)].join(', '));\nelse if (radii.length > 0) passed.push('Border-radius within brand limits');\n\n// 6. Check email width\nconst widthMatch = BRAND_CSS.match(/max-width:\\s*(\\d+)px/);\nconst maxW = widthMatch ? widthMatch[1] : '600';\nif (new RegExp('(?:max-)?width[\\\\s:=]+[\"\\']?' + maxW, 'i').test(html)) passed.push(maxW + 'px email width');\nelse warnings.push(maxW + 'px width not detected');\n\n// 7. Parse tone, do's, don'ts from CSS comments\nconst toneM = BRAND_CSS.match(/── Tone of Voice ──\\s*([\\s\\S]*?)──/);\nconst toneLines = toneM ? toneM[1].trim().split('\\n').map(l => l.trim()).filter(l => l) : [];\nconst dosM = BRAND_CSS.match(/Do's:\\s*([\\s\\S]*?)Don't:/);\nconst dos = dosM ? (dosM[1].match(/\\*\\s+(.+)/g) || []).map(d => d.replace(/^\\*\\s+/, '')) : [];\nconst donM = BRAND_CSS.match(/Don't:\\s*([\\s\\S]*?)\\*\\//);\nconst donts = donM ? (donM[1].match(/\\*\\s+(.+)/g) || []).map(d => d.replace(/^\\*\\s+/, '')) : [];\n\n// Build report\nconst st = issues.length === 0 ? 'ALL CHECKS PASSED' : 'ISSUES FOUND';\nlet r = 'BRAND GUIDELINES CHECK REPORT\\n\\nStatus: ' + st + '\\nPassed: ' + passed.length + ' | Issues: ' + issues.length + ' | Warnings: ' + warnings.length + '\\n\\n';\nif (passed.length > 0) r += 'PASSED:\\n' + passed.map(p => '  + ' + p).join('\\n') + '\\n\\n';\nif (issues.length > 0) r += 'ISSUES (must fix):\\n' + issues.map(i => '  - ' + i).join('\\n') + '\\n\\n';\nif (warnings.length > 0) r += 'WARNINGS (should fix):\\n' + warnings.map(w => '  ! ' + w).join('\\n') + '\\n\\n';\nif (toneLines.length > 0) r += 'TONE OF VOICE:\\n' + toneLines.map(t => '  ' + t).join('\\n') + '\\n\\n';\nif (dos.length > 0) r += \"DO'S:\\n\" + dos.map(d => '  + ' + d).join('\\n') + '\\n\\n';\nif (donts.length > 0) r += \"DON'TS:\\n\" + donts.map(d => '  - ' + d).join('\\n') + '\\n';\nreturn r;